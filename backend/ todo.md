# todo

1. have error handling managed by createServer.js, by throwing errors in the stores
2. clean up functions in create server by creating cookie store
3. have createServer handle checking of body elements and session requirements